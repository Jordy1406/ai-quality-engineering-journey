[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "llm-qa"
version = "0.1.0"
description = "Learning journey from QA Automation to AI Quality Engineering"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
